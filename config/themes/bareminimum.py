theme_name = "bareminimum"

theme_data = {
    # Use pure black for background
    "bg": (0, 0, 0),
    # Use pure white for text
    "fg": (255, 255, 255),
    # For selection, also black or dark gray (to differentiate slightly):
    "sel": (40, 40, 40),
    # Accent color still white, or any mild alternative
    "accent": (255, 255, 255),
    # file tree background
    "ft_bg": (0, 0, 0),
    # sidebar background
    "sidebar": (0, 0, 0),
    # highlight color (maybe a middle gray to stand out slightly)
    "highlight": (100, 100, 100)
}
